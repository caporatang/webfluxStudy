package com.example.webfluxstudy.completableFuture.completableFuture.runAsync;

/**
 * packageName : com.example.webfluxstudy.completableFuture.completableFuture.runAsync
 * fileName : RunAsync
 * author : taeil
 * date : 2023/10/12
 * description :
 * =======================================================
 * DATE          AUTHOR                      NOTE
 * -------------------------------------------------------
 * 2023/10/12        taeil                   최초생성
 */
public class RunAsync {
    // Runnable을 제공하여 CompletableFuture를 생성할 수 있다. -> 구체적인 값을 내리는것이 아니라 Runnable에 주어진 액션이 종료되서 체이닝을 해서 작업을 처리한다 -> 사건을 전달한다.
    // 값을 반환하지 않는다.
    // 다음 task에 null이 전달된다.
}